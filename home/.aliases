alias server="rvm use && rails s"
alias console="rvm use && rails c"
alias migrate="rake db:migrate"
alias wthr="curl 'wttr.in/Pelotas?0'"
alias did="vim +'normal Go' +'r!date' ~/docs/did.txt"
alias todo="todo.sh"
alias mutte="mutt -F ~/.eletrotupi"
alias glow="\glow -p -w 72"

alias j="z"

alias vim=nvim
alias ls="ls --color=auto --group-directories-first"
alias gsummary="git log --author=pedrolucasporcellis@gmail.com --format='- %B' --since=20days --reverse"

# Git related

alias gds="git diff --staged"
alias gd="git diff"
alias gdc="git diff --cached"
alias gp="git push"
alias glog="git log --pretty"
